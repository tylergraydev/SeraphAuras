if not M33Auras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "ptBR" then
  return
end

local L = M33Auras.L

--@localization(locale="ptBR", format="lua_additive_table", namespace="M33Auras / Templates")@
