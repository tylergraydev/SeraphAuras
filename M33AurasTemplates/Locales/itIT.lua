if not M33Auras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "itIT" then
  return
end

local L = M33Auras.L

--@localization(locale="itIT", format="lua_additive_table", namespace="M33Auras / Templates")@
