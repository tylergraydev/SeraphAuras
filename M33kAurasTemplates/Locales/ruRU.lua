if not M33kAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "ruRU" then
  return
end

local L = M33kAuras.L

--@localization(locale="ruRU", format="lua_additive_table", namespace="M33kAuras / Templates")@
