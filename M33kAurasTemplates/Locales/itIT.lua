if not M33kAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "itIT" then
  return
end

local L = M33kAuras.L

--@localization(locale="itIT", format="lua_additive_table", namespace="M33kAuras / Templates")@
