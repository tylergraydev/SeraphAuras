if not M33kAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "ptBR" then
  return
end

local L = M33kAuras.L

--@localization(locale="ptBR", format="lua_additive_table", namespace="M33kAuras / Templates")@
